"use client";
import Link from "next/link"; import { useState } from "react"; import { useRouter } from "next/navigation"; import Navbar from "../../components/Navbar";
export default function LoginPage(){const router=useRouter(); const [role,setRole]=useState("customer"); const [identifier,setIdentifier]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); async function submit(e){e.preventDefault();setError("");setLoading(true);try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identifier,password,role})});const d=await r.json();if(!r.ok)throw new Error(d.error||"No se pudo iniciar sesión."); router.push(d.role==="admin"?"/admin":"/perfil");router.refresh();}catch(err){setError(err.message)}finally{setLoading(false)}} return <main className="min-h-screen bg-white text-gray-900"><Navbar/><section className="max-w-md mx-auto px-6 py-16"><h1 className="text-4xl font-extrabold">Ingresar</h1><div className="mt-6 grid grid-cols-2 gap-2"><button type="button" onClick={()=>setRole("customer")} className={`p-3 rounded border ${role==="customer"?"bg-black text-white":"bg-white"}`}>Cliente</button><button type="button" onClick={()=>setRole("company")} className={`p-3 rounded border ${role==="company"?"bg-black text-white":"bg-white"}`}>Empresa</button></div><form onSubmit={submit} className="mt-6 space-y-4"><input className="w-full border p-3 rounded" placeholder="Usuario o email" value={identifier} onChange={e=>setIdentifier(e.target.value)} required/><input className="w-full border p-3 rounded" type="password" placeholder="Contraseña" value={password} onChange={e=>setPassword(e.target.value)} required/><button disabled={loading} className="w-full bg-yellow-400 text-black px-5 py-3 rounded font-semibold">{loading?"Ingresando...":"Ingresar"}</button>{error?<p className="text-sm text-red-600">{error}</p>:null}</form><p className="mt-6 text-sm text-gray-600">¿No tenés cuenta? <Link href="/registro" className="font-semibold text-black">Creá tu perfil</Link></p></section></main>}
