"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export default function AdminLogin(){const router=useRouter();const [identifier,setIdentifier]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");async function submit(e){e.preventDefault();setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identifier,password,role:"admin"})});const d=await r.json();if(!r.ok){setError(d.error||"No se pudo iniciar sesión.");return}router.push("/admin");router.refresh()}return <main className="min-h-screen bg-black text-white flex items-center justify-center px-6"><form onSubmit={submit} className="w-full max-w-md bg-white text-black p-8 rounded-lg"><h1 className="text-3xl font-extrabold">Administración</h1><p className="mt-2 text-gray-600">Acceso interno.</p><div className="mt-6 space-y-4"><input className="w-full border p-3 rounded" placeholder="Usuario o email" value={identifier} onChange={e=>setIdentifier(e.target.value)} required/><input className="w-full border p-3 rounded" type="password" placeholder="Contraseña" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="w-full bg-yellow-400 text-black px-5 py-3 rounded font-semibold">Ingresar</button>{error?<p className="text-sm text-red-600">{error}</p>:null}</div></form></main>}
